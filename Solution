public class Prime {
  public static boolean isPrime(int num) {
    boolean primo = true;
  
    if(num <=1) return primo =false;
    for(int i = 2; i <= Math.sqrt(num); i++){
      if(num % i == 0){
        primo = false;
        }
      }
    
    return primo;   
    
  }
}
